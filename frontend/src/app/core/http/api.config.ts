/**
 * URL base provisional del backend en desarrollo. Se sustituirá cuando
 * exista configuración definitiva de despliegue (environments/.env del
 * frontend) — no crear esa infraestructura todavía sin necesidad real.
 */
export const API_BASE_URL = 'http://localhost:3000/api';
